g++ -std=c++14 -O2 -Wall benchmark_serial.cpp -o benchmark_serial

g++ -std=c++14 -O2 -Wall -fopenmp benchmark_parallel.cpp -o benchmark_parallel

# Set number of threads for parallel execution
export OMP_NUM_THREADS=4

# Or set it when running:
OMP_NUM_THREADS=4 ./benchmark_parallel

# 1. Navigate to your project directory
cd /path/to/your/project

# 2. Compile both versions
g++ -std=c++14 -O2 -Wall benchmark_serial.cpp -o benchmark_serial
g++ -std=c++14 -O2 -Wall -fopenmp benchmark_parallel.cpp -o benchmark_parallel

# 3. Run serial benchmark
./benchmark_serial

# 4. Run parallel benchmark with 4 threads
OMP_NUM_THREADS=4 ./benchmark_parallel

# 5. Check results
ls -la benchmark_results_*.csv